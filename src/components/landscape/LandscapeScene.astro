---
import HeadwatersScene from "./scenes/HeadwatersScene.astro";
import RiverScene from "./scenes/RiverScene.astro";

interface Props {
    placeId: string;
}

const { placeId } = Astro.props;
---

{
    placeId === "headwaters" && (
        <HeadwatersScene />
    )
}

{
    placeId === "river" && (
        <RiverScene />
    )
}
